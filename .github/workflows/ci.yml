name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          show-progress: false
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Capture Version 1 and Version 2 repository-object integrity
        id: audit
        shell: pwsh
        run: |
          python scripts/verify_v2_final_audit.py *> v2-final-audit-exact.log
          $code = $LASTEXITCODE
          Get-Content v2-final-audit-exact.log
          "exit_code=$code" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          exit 0
      - name: Upload exact audit result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v2-final-audit-exact
          path: v2-final-audit-exact.log
          if-no-files-found: error
          retention-days: 1
      - name: Enforce Version 1 and Version 2 repository-object integrity
        shell: pwsh
        run: |
          if ("${{ steps.audit.outputs.exit_code }}" -ne "0") {
            throw "Consolidated audit failed."
          }
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - name: Verify public release audit
        run: python scripts/audit_public_release.py
      - name: Test portable functional and evidence suite
        env:
          MPLBACKEND: Agg
          PYTHONNOUSERSITE: "1"
          OMP_NUM_THREADS: "1"
          OPENBLAS_NUM_THREADS: "1"
          MKL_NUM_THREADS: "1"
          NUMEXPR_NUM_THREADS: "1"
        run: >
          python -m pytest -q
          --ignore=tests/test_v2_d0_lock.py
          --ignore=tests/test_v2_d1_lock.py
          --ignore=tests/test_v2_d2a_lock.py
          --ignore=tests/test_v2_d2b_lock.py
          --ignore=tests/test_v2_d2c_lock.py
          --ignore=tests/test_v2_d3_lock.py
          --ignore=tests/test_v2_d4_lock.py
          --ignore=tests/test_v2_d5_lock.py
          --ignore=tests/test_v2_d3_assets.py
          --ignore=tests/test_v2_d4_assets.py
          --ignore=tests/test_v2_d5_assets.py
          -k "not test_v2_protocol_lock_hashes"
